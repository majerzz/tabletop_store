import { css } from '@emotion/css'

export const styles = {
  cartGameCard: css`
    display: flex;
    gap: 1vw;
    background-color: rgba(0,0,0,0.1);
    min-width: 50%;
    padding: 2vh;
    align-items: center;
  `,
  cartGameImage: css`
    border-radius: 1%;
    height: 10vh;
    width: 10vw;
    width: auto;
  `
}
