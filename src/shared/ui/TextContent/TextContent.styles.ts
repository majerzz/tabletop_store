import { css } from '@emotion/css'

export const styles = {
  textContent: css`
    display: flex;
    flex-direction: column;
    font-size: 1rem;
    color: #fff;
    text-shadow: 0px 0px 4px rgba(0, 0, 0, 0.4);
    gap: 0.8em;
    font-weight: 400;
  `
}
