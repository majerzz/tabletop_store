import { css } from '@emotion/css'

export const styles = {
  cartPage: css`
    display: flex;
    flex-direction: column;
    align-items: center;
    max-width: 100%;
    margin: 0 auto;
    gap: 16px;
  `
}
