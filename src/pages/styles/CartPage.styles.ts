import { css } from '@emotion/css'

export const styles = {
  cartPage: css`
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    width: fit-content;
    margin: 0 auto;
    gap: 16px;
    padding: 0 20px;
  `
}
