import { css } from '@emotion/css'

export const styles = {
  cartPage: css`
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    width: fit-content;
    margin: 0 auto;
    gap: 2vh;
    padding: 0 20vw;
  `
}
